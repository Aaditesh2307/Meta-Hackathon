{
  "task_id": "easy",
  "description": "Simple PR Bug Fix: Find the logic error in the codebase and submit a patch.",
  "episodes": [
    {
      "seed": 42,
      "current_files": {
        "utils.py": "def calculate_total(items, tax_rate=0.1):\n    \"\"\"Calculate the total price with tax.\"\"\"\n    # Calculate subtotal first\n    subtotal = sum(item[\"price\"] + item[\"quantity\"] for item in items)\n    \n    # Apply tax\n    tax = subtotal * tax_rate\n    total = subtotal + tax\n    return round(total, 2)\n"
      },
      "pr_diff": "--- a/utils.py\n+++ b/utils.py\n@@ -1,7 +1,7 @@\n def calculate_total(items, tax_rate=0.1):\n     \"\"\"Calculate the total price with tax.\"\"\"\n     # Calculate subtotal first\n-    subtotal = sum(item[\"price\"] * item[\"quantity\"] for item in items)\n+    subtotal = sum(item[\"price\"] + item[\"quantity\"] for item in items)\n     \n     # Apply tax\n     tax = subtotal * tax_rate",
      "ground_truth": {
        "utils.py": "def calculate_total(items, tax_rate=0.1):\n    \"\"\"Calculate the total price with tax.\"\"\"\n    # Calculate subtotal first\n    subtotal = sum(item[\"price\"] * item[\"quantity\"] for item in items)\n    \n    # Apply tax\n    tax = subtotal * tax_rate\n    total = subtotal + tax\n    return round(total, 2)\n"
      },
      "test_suite": {
        "test_calculate_total": "def test_calculate_total():\n    items = [{\"price\": 10.0, \"quantity\": 2}, {\"price\": 5.0, \"quantity\": 3}]\n    result = calculate_total(items, tax_rate=0.1)\n    assert result == 38.5, f\"Expected 38.5, got {result}\"\n    return True\n"
      }
    }
  ]
}